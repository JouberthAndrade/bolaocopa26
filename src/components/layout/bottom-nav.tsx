import { PrimaryNav } from "@/components/layout/primary-nav";

/** Barra de navegação inferior (mobile). Esconde em md+ — ver PrimaryNav. */
export function BottomNav() {
  return <PrimaryNav variant="bottom" />;
}
